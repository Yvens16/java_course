package outils;

public class Math_5 {
  // Math.max(1, 2); // retourne le plus grand
  // Math.min(1, 2); // Retourne le plus petit
  // Math.sqrt(64); // Retourne racine
  // Maths.abs(-4); // Retourne la valeur positive absolue
  // Math.random(); // retourne une valeur aléatoire entre 0.0 et 1.0 exclusif, donc ça ne peut retourner 1
  // int random = (int)(Math.random() * 101); // why 101 because it satrt at 0 so 0 to 99 if i use 100 
}
